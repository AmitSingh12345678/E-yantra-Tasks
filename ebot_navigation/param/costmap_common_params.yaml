
footprint: [[-0.3, -0.25], [-0.3, 0.25], [0.3, 0.25], [0.3, -0.25]]

obstacle_layer:
  enabled:              true
  # max_obstacle_height:  2.0
  obstacle_range: 2.5
  raytrace_range: 3.0
  observation_sources:  scan 
  scan:
    data_type: LaserScan
    topic: scan
    marking: true
    clearing: true
    # min_obstacle_height: 0.0
    # max_obstacle_height: 3.0
  

inflation_layer:
  enabled:              true
  cost_scaling_factor:  5.0  
  inflation_radius:     0.55  # it should be 0.55

static_layer:
  enabled:              true
  

